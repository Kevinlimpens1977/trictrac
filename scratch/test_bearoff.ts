import { createInitialState } from '../src/engine/gameReducer'; import { canBearOff } from '../src/engine/moveEngine'; const s = createInitialState(); s.setupCountB = 15; s.setupCountW = 15; s.points[19] = {owner:'B',count:3}; s.points[20] = {owner:'B',count:3}; s.points[21] = {owner:'B',count:3}; s.points[22] = {owner:'B',count:2}; s.points[23] = {owner:'B',count:2}; s.points[24] = {owner:'B',count:2}; console.log('canBearOff(B):', canBearOff(s, 'B'));
